# Phase VI-B/C plan

Externally grounded trajectory and observation-ablation validation using the pinned public AI data-center dataset already recorded in the repository.

The analysis will preserve upstream field names and will not assign undocumented physical units to the source `Size` field.
